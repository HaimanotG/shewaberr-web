import React from 'react';
//import Styles from './contactustemplate__form__details.module.sass';

export const ContactupTemplateFormFields = ({ children, className =''}) => (
    <div className={className}>
        {children}
    </div>
)