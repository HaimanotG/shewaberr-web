import { Search as Base } from './Search';
import { SearchLeft } from './__left/Search__left';
import { SearchRight } from './__right/Search__right';

export const Search = Base;

export {
    SearchLeft,
    SearchRight
}