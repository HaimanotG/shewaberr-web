import { Hero as Base } from './Hero';
import { HeroElement } from './__element/Hero__element';
import { HeroElementStart } from './__elment_start/Hero__element_start';

export const Hero = Base;

export {
    HeroElement,
    HeroElementStart
}