import { Activity as Base } from './Activity';
import { ActivityImage } from './__image/Activity__image';
import { ActivityContent } from './__content/Activity__content';

export const Activity = Base;

export {
    ActivityImage,
    ActivityContent,
}