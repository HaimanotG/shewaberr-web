import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import {
    Hero,
    HeroElement
} from 'components/molecules/Hero';
import ReactPlaceholder from 'react-placeholder';

<Meta title="Molecules/Hero" component={Hero} />

# Hero
<Canvas>
    <Story name="default">
        <Hero>
            <HeroElement>
                <ReactPlaceholder type='rect' ready={false} color='#E0E0E0' style={{ width: '100%', height: '500px' }} />
            </HeroElement>
            <HeroElement>
                <ReactPlaceholder type='rect' ready={false} color='#E0E0E0' style={{ width: '100%', height: '500px' }} />
            </HeroElement>
        </Hero>
    </Story>
</Canvas>
