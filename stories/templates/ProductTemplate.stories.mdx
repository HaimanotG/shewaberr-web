import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { ProductTemplate, ProductTemplateBody } from 'components/templates/ProductTemplate';
import ReactPlaceholder from 'react-placeholder';
import { Navbar } from 'components/organisms/Navbar';
import { Nav, NavItem } from 'components/molecules/Nav';

<Meta title="Templates/ProductTemplate" component={ProductTemplate} />

# ProductTemplate
<Canvas>
    <Story name="default">
        <ProductTemplate>
            <Navbar background="orange">
                <ReactPlaceholder type='rect' ready={false} color='#E0E0E0' style={{ width: 200, height: 35 }} />
                <ReactPlaceholder type='rect' ready={false} color='#E0E0E0' style={{ width: 900, height: 35 }} />
            </Navbar>
            <ProductTemplateBody>
                df
            </ProductTemplateBody>
        </ProductTemplate>
    </Story>
</Canvas>